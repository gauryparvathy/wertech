{
  "name": "wertech-root",
  "private": true,
  "scripts": {
    "build": "npm install --prefix wertech-app && npm install --prefix server && npm run build --prefix wertech-app",
    "start": "npm start --prefix server"
  }
}
